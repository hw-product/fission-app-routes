:ruby
  id = 'new-payload-match-rule'
  content_for(:container) do
    render(
      :partial => 'layouts/panel',
      :locals => {
        :id => id,
        :heading => 'New Payload Match Rule',
        :panel_content => render(
          :partial => 'form',
          :locals => {
            :form_opts => {
              :url => admin_payload_match_rules_path,
              :method => :post
            }
          }
        )
      }
    )
  end

= render(:partial => 'layouts/container', :locals => {:container_class => "#{id}-form", :container_id => id})
